# Test environment defaults (overridden by Testcontainers where needed)
ENVIRONMENT=test

# Security (safe for tests)
SECURITY_KEY="test_secret_key"

# Database (host/port overridden by Testcontainers)
POSTGRES_SERVER=localhost
POSTGRES_PORT=0
POSTGRES_DB=test
POSTGRES_USER=test
POSTGRES_PASSWORD=test

# Redis (not used in most unit tests)
REDIS_URL="memory://"

# API Keys
API_KEY_LOOKUP_PREFIX_LENGTH=8

# Connectivity
BACKEND_CORS_ORIGINS="http://localhost:3000"
TRUSTED_HOSTS="test"

# Project
PROJECT_NAME="Test Analytics"
PROJECT_DESCRIPTION="Testing Description"
